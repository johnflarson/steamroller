---
phase: 04-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project.godot
  - export_presets.cfg
  - .gitignore
  - deploy.sh
  - /home/jlarson/code/luminaldata-www/src/content/blog/steamroller.md
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02

must_haves:
  truths:
    - "project.godot config/name is 'Steamroller'"
    - "export_presets.cfg has Web, Linux, and Windows presets with correct paths"
    - "export/linux/ and export/windows/ directories are gitignored"
    - "deploy.sh automates HTML5 copy to Astro, butler push, and GitHub Release"
    - "Astro blog post exists with iframe embedding the game at /games/steamroller/"
  artifacts:
    - path: "project.godot"
      provides: "Renamed game config"
      contains: 'config/name="Steamroller"'
    - path: "export_presets.cfg"
      provides: "All three export presets (Web, Linux, Windows)"
      contains: "preset.2"
    - path: ".gitignore"
      provides: "Gitignore for linux and windows export dirs"
      contains: "export/linux/"
    - path: "deploy.sh"
      provides: "One-command release script"
      min_lines: 40
    - path: "/home/jlarson/code/luminaldata-www/src/content/blog/steamroller.md"
      provides: "Astro blog post with game iframe"
      contains: "iframe"
  key_links:
    - from: "deploy.sh"
      to: "/home/jlarson/code/luminaldata-www/public/games/steamroller/"
      via: "cp export/web/* to Astro public dir"
      pattern: "cp.*export/web.*steamroller"
    - from: "deploy.sh"
      to: "itch.io butler push"
      via: "butler push commands for html5, linux, windows channels"
      pattern: "butler push"
    - from: "deploy.sh"
      to: "GitHub Release"
      via: "gh release create with zipped assets"
      pattern: "gh release create"
    - from: "export_presets.cfg"
      to: "export/linux/steamroller.x86_64"
      via: "export_path setting"
      pattern: "export_path.*steamroller"
---

<objective>
Prepare all project configuration, distribution scripts, and hosting content for Steamroller release.

Purpose: Set up everything needed so that after the user manually exports from Godot, a single `deploy.sh` run distributes to all three destinations (Astro site, itch.io, GitHub Releases).
Output: Renamed project, complete export presets, gitignore updates, deploy script, Astro blog post.
</objective>

<execution_context>
@/home/jlarson/.claude/get-shit-done/workflows/execute-plan.md
@/home/jlarson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution/04-CONTEXT.md
@.planning/phases/04-distribution/04-RESEARCH.md

@project.godot
@export_presets.cfg
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename game and configure export presets</name>
  <files>project.godot, export_presets.cfg, .gitignore</files>
  <action>
1. In `project.godot`, change line 13 from `config/name="Dice Grid Game"` to `config/name="Steamroller"`.

2. In `export_presets.cfg`, append Linux and Windows presets after the existing Web preset (preset.0). Use the exact format from the research doc:

   - **preset.1** — Linux: `platform="Linux"`, `export_path="export/linux/steamroller.x86_64"`, `binary_format/embed_pck=true`, `binary_format/architecture="x86_64"`, `texture_format/s3tc_bptc=true`, `texture_format/etc2_astc=false`, `script_export_mode=2`.
   - **preset.2** — Windows Desktop: `platform="Windows Desktop"`, `export_path="export/windows/steamroller.exe"`, `binary_format/embed_pck=true`, `binary_format/architecture="x86_64"`, `texture_format/s3tc_bptc=true`, `texture_format/etc2_astc=false`, `script_export_mode=2`, `application/product_name="Steamroller"`.

   Also update the Web preset name from "Web" to "Steamroller Web" in the `name=` field (preset.0) so all presets carry the game name.

3. In `.gitignore`, append these lines after the existing web export section:

```
# Linux export output
export/linux/

# Windows export output
export/windows/
```

4. Create the `export/linux/` and `export/windows/` directories with `.gdignore` files so Godot doesn't scan them (consistent with how `export/web/` works — Godot needs the directories to exist for export but shouldn't import their contents).
  </action>
  <verify>
    <automated>grep -q 'config/name="Steamroller"' project.godot && grep -q 'preset.1' export_presets.cfg && grep -q 'preset.2' export_presets.cfg && grep -q 'export/linux/' .gitignore && grep -q 'export/windows/' .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>project.godot says "Steamroller", export_presets.cfg has all 3 presets with correct paths and embed_pck=true, .gitignore covers linux and windows export dirs, export directories exist</done>
</task>

<task type="auto">
  <name>Task 2: Create deploy script and Astro blog post</name>
  <files>deploy.sh, /home/jlarson/code/luminaldata-www/src/content/blog/steamroller.md</files>
  <action>
1. Create `deploy.sh` in the project root with these characteristics (per user decision — single all-in-one script):

   - `#!/usr/bin/env bash` with `set -euo pipefail`
   - **Version auto-increment**: Read last git tag (`git describe --tags --abbrev=0`, default `v0.9.9` if none), parse semver, increment patch
   - **Validate exports exist**: Check that `export/web/index.html`, `export/linux/steamroller.x86_64`, and `export/windows/steamroller.exe` all exist. Exit with clear error message if any missing, telling user to export from Godot first
   - **Validate tools**: Check that `butler` and `gh` are on PATH; check that `gh auth status` succeeds; print actionable error messages if not
   - **Step 1 — Copy HTML5 to Astro repo**: `mkdir -p` the target dir at `/home/jlarson/code/luminaldata-www/public/games/steamroller/`, copy `export/web/*` there, cd to Astro repo, `git add public/games/steamroller/`, commit with version in message (use `|| true` so re-runs don't fail on empty commit), push to main, cd back
   - **Step 2 — Deploy Astro site**: cd to Astro repo, run `bash deploy.sh`, cd back. Print note that this requires SSH access to luminaldata-prod
   - **Step 3 — Push to itch.io via butler**: Three `butler push` commands for html5, linux, windows channels. Use placeholder `ITCH_USERNAME` with a comment telling user to replace it. Use `--userversion "${VERSION#v}"` (strip v prefix for itch.io)
   - **Step 4 — Package for GitHub Release**: Create temp dir, `zip -j` linux and windows builds with version in filename
   - **Step 5 — Create GitHub Release**: `git tag`, `git push origin` the tag, `gh release create` with both zips as assets, title "Steamroller $VERSION", notes linking to luminaldata.com play URL
   - **Step 6 — Cleanup**: Remove temp zip dir
   - Print summary at end with version number and links

   Make the script executable (`chmod +x`).

2. Create the Astro blog post at `/home/jlarson/code/luminaldata-www/src/content/blog/steamroller.md`:

   - Use the same frontmatter format as existing posts (title, description, author, date, image, imageAlt, tags, linkedin, bluesky)
   - Title: "Steamroller — a browser dice strategy game"
   - Description: "Roll dice, claim cells, form lines. A turn-based strategy game for 2-4 players."
   - Author: "John Larson"
   - Date: 2026-03-14
   - No image field (omit — no screenshot for v1 per deferred decisions)
   - Tags: ["game", "godot", "dice", "strategy", "browser game"]
   - linkedin: false, bluesky: false
   - Body: Brief intro paragraph explaining the game, then a centered div with iframe at `/games/steamroller/index.html`, 1280x720, `style="border:none;max-width:100%;"`, `allow="fullscreen"`
  </action>
  <verify>
    <automated>test -x deploy.sh && grep -q "butler push" deploy.sh && grep -q "gh release create" deploy.sh && grep -q "ITCH_USERNAME" deploy.sh && test -f /home/jlarson/code/luminaldata-www/src/content/blog/steamroller.md && grep -q "iframe" /home/jlarson/code/luminaldata-www/src/content/blog/steamroller.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>deploy.sh is executable with all 5 steps (Astro copy, Astro deploy, itch.io butler push, GitHub Release packaging, GitHub Release creation), version auto-increment from git tags, validation checks for missing builds and tools. Astro blog post has correct frontmatter and iframe embed.</done>
</task>

</tasks>

<verification>
- `grep 'config/name' project.godot` shows "Steamroller"
- `grep -c 'preset\.' export_presets.cfg` shows 3 preset sections (0, 1, 2)
- `cat .gitignore` includes linux and windows export dirs
- `bash -n deploy.sh` passes syntax check (no execution)
- Blog post at luminaldata-www has iframe pointing to /games/steamroller/
</verification>

<success_criteria>
All configuration files updated for "Steamroller" name. Three export presets configured (Web, Linux, Windows) with correct output paths and embed_pck=true. Deploy script handles the full release pipeline with proper validation. Astro blog post ready for game embed. User can now export from Godot editor and run deploy.sh to distribute.
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-01-SUMMARY.md`
</output>
