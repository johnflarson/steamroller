---
phase: 04-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [export_presets.cfg, deploy.sh]
autonomous: true
gap_closure: true
requirements: [EXPORT-01, EXPORT-02]

must_haves:
  truths:
    - "Linux and Windows presets have embed_pck=true for single-file distribution"
    - "Windows preset includes product_name Steamroller"
    - "Web preset is named Steamroller Web"
    - "deploy.sh has no misleading TODO comment on ITCH_USERNAME line"
  artifacts:
    - path: "export_presets.cfg"
      provides: "Corrected export presets with embed_pck=true, product name, preset name"
      contains: "embed_pck=true"
    - path: "deploy.sh"
      provides: "Clean deploy script without misleading TODO"
  key_links:
    - from: "export_presets.cfg"
      to: "deploy.sh"
      via: "embed_pck=true means zip -j only needs the binary (no .pck companion)"
      pattern: "embed_pck=true"
---

<objective>
Fix export preset and deploy script gaps found during Phase 04 verification.

Purpose: The Linux and Windows export presets have embed_pck=false instead of true, meaning builds produce a binary+pck pair rather than a single self-contained executable. This causes GitHub Release zips (which only include the binary) to be incomplete. Additionally, the Windows preset is missing the product_name metadata, the Web preset is not renamed to "Steamroller Web", and deploy.sh has a misleading TODO comment.

Output: Corrected export_presets.cfg and deploy.sh ready for re-export and re-deploy.
</objective>

<execution_context>
@/home/jlarson/.claude/get-shit-done/workflows/execute-plan.md
@/home/jlarson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution/04-VERIFICATION.md

<interfaces>
From export_presets.cfg (current state needing fixes):
- Line 3: name="Web" --> should be "Steamroller Web"
- Line 80: binary_format/embed_pck=false --> should be true (Linux preset)
- Line 127: binary_format/embed_pck=false --> should be true (Windows preset)
- Line 145: application/product_name="" --> should be "Steamroller"

From deploy.sh:
- Line 21: ITCH_USERNAME="johnfredone"  # TODO: replace with your actual itch.io username
  The value is already set correctly; remove the misleading TODO comment.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix export_presets.cfg — embed_pck, product_name, preset name</name>
  <files>export_presets.cfg</files>
  <action>
Edit export_presets.cfg to make these exact changes:

1. Line 3: Change `name="Web"` to `name="Steamroller Web"`
2. Line 80: Change `binary_format/embed_pck=false` to `binary_format/embed_pck=true` (Linux preset.1.options)
3. Line 127: Change `binary_format/embed_pck=false` to `binary_format/embed_pck=true` (Windows preset.2.options)
4. Line 145: Change `application/product_name=""` to `application/product_name="Steamroller"` (Windows preset.2.options)

Do NOT change any other values. These four edits are the only changes needed.

After this fix, re-exporting from Godot will produce single-file executables (no separate .pck), which means the existing deploy.sh zip commands that package only the binary are correct — no deploy.sh zip change needed.
  </action>
  <verify>
    <automated>grep -n 'embed_pck' export_presets.cfg | grep -c 'true' | grep -q '2' && grep -n 'product_name="Steamroller"' export_presets.cfg | grep -q 'Steamroller' && grep -n 'name="Steamroller Web"' export_presets.cfg | grep -q 'Steamroller Web' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both Linux and Windows presets have embed_pck=true, Windows preset has product_name="Steamroller", Web preset is named "Steamroller Web"</done>
</task>

<task type="auto">
  <name>Task 2: Remove misleading TODO comment from deploy.sh</name>
  <files>deploy.sh</files>
  <action>
Edit deploy.sh line 21 to remove the misleading TODO comment. Change:
  ITCH_USERNAME="johnfredone"  # TODO: replace with your actual itch.io username
To:
  ITCH_USERNAME="johnfredone"

The username is already correctly set. The TODO comment is misleading for anyone reading the script.
  </action>
  <verify>
    <automated>grep -c 'TODO' deploy.sh | grep -q '0' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>deploy.sh has no misleading TODO comment; ITCH_USERNAME value is preserved as "johnfredone"</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep 'embed_pck=true' export_presets.cfg` returns 2 lines (Linux and Windows)
2. `grep 'embed_pck=false' export_presets.cfg` returns 0 lines
3. `grep 'product_name="Steamroller"' export_presets.cfg` returns 1 line
4. `grep 'name="Steamroller Web"' export_presets.cfg` returns 1 line
5. `grep 'TODO' deploy.sh` returns 0 lines
6. `grep 'ITCH_USERNAME="johnfredone"' deploy.sh` returns 1 line (value preserved)

NOTE: After these config changes, the user must re-export from Godot editor (Project > Export > Export All) to produce new single-file binaries with embedded .pck, then re-run deploy.sh to update all distribution channels.
</verification>

<success_criteria>
- export_presets.cfg has embed_pck=true on both Linux and Windows presets
- Windows preset has application/product_name="Steamroller"
- Web preset is named "Steamroller Web"
- deploy.sh has no TODO comments
- No other values in either file are changed
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-03-SUMMARY.md`
</output>
