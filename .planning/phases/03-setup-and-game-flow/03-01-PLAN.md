---
phase: 03-setup-and-game-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenes/main.tscn
  - scripts/main.gd
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - WIN-03

must_haves:
  truths:
    - "At game launch, a setup screen appears with player count toggles and name fields — not the board"
    - "Selecting 2/3/4 players shows/hides the correct name fields with slide animation"
    - "Name fields are pre-filled with defaults, max 15 chars, Enter chains to next field"
    - "Empty name fields get a random fun name on Start Game"
    - "Start Game hides setup, shows board configured for the selected player count and entered names"
    - "Win overlay New Game button returns to setup screen with previous names and count preserved"
    - "Transitions between setup and game use a quick fade (~0.2s)"
  artifacts:
    - path: "scenes/main.tscn"
      provides: "SetupOverlay node tree with Dimmer, SetupCard, CountRow, NamesContainer, PlayerRow0-3, NameInput0-3, StartButton"
      contains: "SetupOverlay"
    - path: "scripts/main.gd"
      provides: "Setup screen logic, dynamic player init, Play Again reroute"
      exports: ["_init_setup_screen", "_on_start_game_pressed", "_on_count_selected", "_fade_to_setup", "_fade_to_game", "_random_fun_name"]
  key_links:
    - from: "scripts/main.gd (_on_start_game_pressed)"
      to: "players array + player_count"
      via: "reads LineEdit.text and ButtonGroup selection to build dynamic players array"
      pattern: "players\\.append.*name_inputs"
    - from: "scripts/main.gd (_on_new_game_pressed)"
      to: "scripts/main.gd (_fade_to_setup)"
      via: "New Game button routes to setup screen instead of resetting in place"
      pattern: "_fade_to_setup"
    - from: "scripts/main.gd (_ready)"
      to: "SetupOverlay visible"
      via: "_ready shows setup overlay, hides HBoxContainer"
      pattern: "setup_overlay\\.visible.*true"
---

<objective>
Build the setup screen (player count selection + name entry) and wire it into the game flow so the complete player journey works: setup -> play -> win -> setup.

Purpose: Completes the player-facing experience — users can choose how many players, name them, play a full game, and return to setup without page reload. This is the last gameplay feature before distribution.
Output: Modified main.tscn with SetupOverlay node tree; modified main.gd with setup logic, dynamic player initialization, and Play Again rerouting.
</objective>

<execution_context>
@/home/jlarson/.claude/get-shit-done/workflows/execute-plan.md
@/home/jlarson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-setup-and-game-flow/03-CONTEXT.md
@.planning/phases/03-setup-and-game-flow/03-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs. Extracted from scripts/main.gd -->

Key constants (reuse for setup styling):
```gdscript
const PLAYER_COLORS := [Color(0.878,0.439,0.376), Color(0.376,0.502,0.784), Color(0.439,0.659,0.439), Color(0.831,0.627,0.251)]
const DARK_BG := Color(0.13, 0.13, 0.16)
const SIDEBAR_BG := Color(0.17, 0.17, 0.21)
const ACCENT_GOLD := Color(0.90, 0.75, 0.25)
const NEUTRAL_CELL := Color(0.22, 0.22, 0.27)
```

Reusable styling helpers:
```gdscript
func _style_button_gold(btn: Button) -> void   # Gold accent for Start Game + selected count toggle
func _set_cell_color(btn: Button, bg: Color, border_color: Color = Color.TRANSPARENT, border_px: int = 0) -> void
```

Existing state vars (modify, don't duplicate):
```gdscript
var players: Array = [...]  # Currently hardcoded 4 entries — make dynamic from setup
var player_count := 4       # Currently defaults to 4 — set from setup selection
```

Existing functions to modify:
```gdscript
func _ready() -> void          # Must show setup first instead of going to gameplay
func _on_new_game_pressed()    # Must route to setup screen instead of resetting in place
func _setup_score_strip()      # Already clears arrays; safe to call with new player_count
```

Scene tree (current):
```
Main (PanelContainer)
  HBoxContainer
    BoardPanel (VBoxContainer) -> GridContainer, ScoreStrip
    Sidebar (PanelContainer) -> SidebarContent -> CurrentPlayerBadge, RollResultLabel, RollButton, LogScroll/GameLog
  WinOverlay (Control, visible=false) -> Dimmer, Panel/VBox -> TitleLabel, ScoresContainer, NewGameButton
```

CRITICAL constraints:
- Never call _build_grid() after _ready() — double-fires signals on 100 cell buttons
- Use explicit GDScript type annotations (var x: Type = ...) for web export compatibility
- queue_free() on score strip children before _setup_score_strip() when player count changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SetupOverlay to scene and build setup screen UI + logic</name>
  <files>scenes/main.tscn, scripts/main.gd</files>
  <action>
**Scene changes (main.tscn):**
Add a SetupOverlay node as a sibling of WinOverlay under Main. Structure:

```
SetupOverlay (Control) — anchor_right=1.0, anchor_bottom=1.0, visible=true
  Dimmer (ColorRect) — anchor_right=1.0, anchor_bottom=1.0, color=Color(0,0,0,0.65)
  SetupCard (PanelContainer) — anchor_left=0.25, anchor_top=0.15, anchor_right=0.75, anchor_bottom=0.85
    VBox (VBoxContainer) — separation=16, expand fill both axes
      CountRow (HBoxContainer) — separation=8, center alignment
        CountBtn2 (Button) — text="2", toggle_mode=true
        CountBtn3 (Button) — text="3", toggle_mode=true
        CountBtn4 (Button) — text="4", toggle_mode=true
      NamesContainer (VBoxContainer) — separation=8
        PlayerRow0 (HBoxContainer)
          NameInput0 (LineEdit)
        PlayerRow1 (HBoxContainer)
          NameInput1 (LineEdit)
        PlayerRow2 (HBoxContainer)
          NameInput2 (LineEdit)
        PlayerRow3 (HBoxContainer)
          NameInput3 (LineEdit)
      StartButton (Button) — text="Start Game"
```

**Script changes (main.gd):**

1. Add @onready references for all new nodes: setup_overlay, count_btn_2/3/4, name_inputs (Array of 4 LineEdit), player_rows (Array of 4 HBoxContainer), start_button.

2. Add random name constants at the top:
```gdscript
const ADJECTIVES := ["Brave", "Lucky", "Swift", "Bold", "Calm", "Fierce", "Jolly", "Keen", "Nimble", "Proud", "Witty", "Zesty"]
const NOUNS := ["Fox", "Bear", "Star", "Wolf", "Hawk", "Lion", "Owl", "Puma", "Rook", "Sage", "Wren", "Lynx"]
```

3. Add `func _random_fun_name() -> String` that picks random adjective + noun.

4. Add `func _init_setup_screen() -> void`:
   - Create a ButtonGroup, assign to all 3 count buttons with toggle_mode=true
   - Connect ButtonGroup.pressed signal to `_on_count_selected`
   - Set count_btn_2.button_pressed = true (default 2 players)
   - For each of the 4 LineEdit name_inputs[i]:
     - Set max_length=15, placeholder_text="Player N", text="Player N"
     - Style with colored border using PLAYER_COLORS[i]: StyleBoxFlat with bg=Color(0.20,0.20,0.25), border_width=2, border_color=PLAYER_COLORS[i], corner_radius=6, content_margin=6
     - Override both "normal" and "focus" styleboxes with same style
     - Set font_color=White, font_placeholder_color=Color(0.6,0.6,0.6)
   - Connect text_submitted on each name_input to chain Enter: fields 0-2 advance focus to next visible field, last visible field calls _on_start_game_pressed
   - Style StartButton with _style_button_gold()
   - Connect StartButton.pressed to _on_start_game_pressed
   - Style count buttons: selected=gold, unselected=neutral (NEUTRAL_CELL bg, white text, rounded corners)
   - Style SetupCard panel with SIDEBAR_BG, corner_radius=10
   - Call _update_name_field_visibility(2) for initial 2-player default
   - Hide player rows 2 and 3 initially

5. Add `func _on_count_selected(btn: BaseButton) -> void`:
   - Get count from int(btn.text)
   - Update all count button styles (pressed=gold, others=neutral via a new helper `_style_button_neutral`)
   - Call _update_name_field_visibility(count) with slide animation

6. Add `func _style_button_neutral(btn: Button) -> void`:
   - StyleBoxFlat with bg=NEUTRAL_CELL, corner_radius=8, white font color
   - Override normal/hover/pressed/disabled states

7. Add `func _update_name_field_visibility(count: int) -> void`:
   - For player_rows[2]: if count >= 3 show with slide animation (_show_player_row), else hide with slide (_hide_player_row)
   - For player_rows[3]: if count >= 4 show, else hide
   - Use Tween on custom_minimum_size and modulate:a per Research Pattern 5 (~0.15s duration, TRANS_SINE easing)

8. Add `func _show_player_row(row: Control) -> void` and `func _hide_player_row(row: Control) -> void` per Research Pattern 5.

9. Add `func _get_selected_count() -> int`:
   - Check which count button has button_pressed=true, return corresponding int (2, 3, or 4)

10. Modify `_ready()`:
    - Keep existing style setup (root panel, grid gaps, sidebar style, button gold, font size)
    - Keep _init_arrays(), _generate_board(), _build_grid()
    - REMOVE the calls to _setup_score_strip() and _update_ui() from _ready (they move to _on_start_game_pressed)
    - Hide HBoxContainer: $HBoxContainer.visible = false
    - SetupOverlay starts visible=true (in scene file)
    - Call _init_setup_screen()
    - Keep roll_button.pressed.connect and new_game_button.pressed.connect and _style_button_gold(new_game_button)

**Important:** Use explicit type annotations everywhere (e.g., `var count: int = int(btn.text)`) for web export. Do NOT use `:=` type inference for variables assigned from array access or method returns.
  </action>
  <verify>
Open project.godot in Godot 4 editor, press F5. Verify: setup screen appears (dark card centered on dark background), three count buttons visible with "2" selected (gold), two name fields visible with "Player 1" and "Player 2" pre-filled with colored borders, Start Game button visible in gold. Click "3" — third name field slides in. Click "2" — it slides out. Click "4" — two fields slide in. Type in name fields — max 15 chars. Press Enter in field 1 — focus moves to field 2.
  </verify>
  <done>Setup screen appears at game launch with player count toggles (2/3/4), name entry fields with colored borders, and Start Game button. Count toggle shows/hides name fields. Enter chains between fields. All styled consistently with existing dark theme.</done>
</task>

<task type="auto">
  <name>Task 2: Wire setup to gameplay and reroute New Game to setup</name>
  <files>scripts/main.gd</files>
  <action>
1. Add `func _on_start_game_pressed() -> void`:
   - Read player_count from _get_selected_count()
   - Build players array dynamically: for each i in player_count, read name_inputs[i].text.strip_edges(). If empty, assign _random_fun_name(). Create dict with name, color=PLAYER_COLORS[i], score=0.
   - Clear and rebuild score strip: iterate score_strip.get_children() calling queue_free(), clear score_labels and score_panels arrays, then call _setup_score_strip()
   - Reset game state: current_player=0, current_roll=0, state=WAIT_ROLL
   - Reset data grids: owner_grid all -1, scored_grid all false
   - Generate fresh board: _generate_board()
   - Update buttons in place (do NOT call _build_grid()): for each r,c set btn.text=str(board_numbers[r][c]), btn.disabled=false, btn.scale=Vector2.ONE, _set_cell_color(btn, NEUTRAL_CELL)
   - Clear game_log
   - Call _fade_to_game() to transition
   - After fade completes, call _update_ui()

2. Add `func _fade_to_game() -> void`:
   - Tween setup_overlay modulate:a from current to 0.0 over 0.2s (TRANS_SINE, EASE_IN)
   - await tween.finished
   - setup_overlay.visible = false
   - Show HBoxContainer: hbox_container.visible = true (use the $HBoxContainer reference)

3. Add `func _fade_to_setup() -> void`:
   - Hide win_overlay: win_overlay.visible = false
   - Set setup_overlay.modulate.a = 0.0, setup_overlay.visible = true
   - Tween modulate:a to 1.0 over 0.2s (TRANS_SINE, EASE_OUT)
   - Hide HBoxContainer: $HBoxContainer.visible = false

4. Modify `_on_new_game_pressed()`:
   - Replace the entire existing body with a single call to _fade_to_setup()
   - The setup screen preserves all previous state (LineEdit.text values persist, ButtonGroup selection persists) — NO re-initialization needed
   - This satisfies WIN-03: Play Again returns to player count/name selection

5. Add an @onready var for HBoxContainer if not already present (currently accessed via $HBoxContainer inline — keep using $ path or add an @onready, either works).

**CRITICAL reminders:**
- Never call _build_grid() — update the existing 100 buttons in place
- Rebuild score strip by queue_free() on children + clear arrays + _setup_score_strip()
- Use explicit type annotations for web export
- The _on_enter_from_field logic (from Task 1) must check _get_selected_count() at runtime to determine if Enter on the current field should start the game or advance focus
  </action>
  <verify>
Open project.godot in Godot 4 editor, press F5. Full flow test: (1) Setup appears. Select 3 players, enter names "Alice", "Bob", "Charlie". Press Start Game — board appears with fade, score strip shows 3 players with entered names, game plays normally. (2) Play until someone wins (or use debugger to set score=4 then score a line). Win overlay appears. Click "New Game" — fades to setup screen with "Alice", "Bob", "Charlie" still in fields and "3" selected. (3) Change to 2 players, clear name field 1, press Start Game — verify random fun name assigned to empty field, score strip shows 2 players. (4) Leave all name fields at defaults, press Start Game — "Player 1" and "Player 2" used as names.
  </verify>
  <done>Start Game reads setup state and launches gameplay with correct player count and names. New Game from win overlay returns to setup screen with prior state preserved. Fade transitions work between setup/game/win states. Random fun names assigned to empty fields. Score strip rebuilds correctly for different player counts between games.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete setup and game flow end-to-end</name>
  <files>scenes/main.tscn, scripts/main.gd</files>
  <action>Human verifies the complete setup screen and game flow works end-to-end. All automation is complete in Tasks 1-2. This checkpoint confirms visual correctness and interactive behavior.</action>
  <verify>
1. Launch game (F5 in Godot editor)
2. Verify setup screen appears centered on dark background — no board visible
3. Verify "2" is selected (gold), two name fields visible with colored borders (coral, slate blue)
4. Click "3" — third field slides in (sage border). Click "4" — fourth slides in (amber border). Click "2" — fields 3/4 slide out
5. Type a long name (>15 chars) — verify truncation at 15
6. Press Enter in field 1 — focus moves to field 2. Press Enter on last visible field — game starts
7. Verify board appears with fade, score strip shows correct player count and names
8. Play a game to completion (win or stalemate)
9. On win overlay, verify "New Game" button present
10. Click "New Game" — verify fade back to setup screen with previous names and count preserved
11. Change player count, start again — verify score strip rebuilds correctly
12. Clear a name field, start game — verify random fun name appears in score strip
  </verify>
  <done>All 12 verification steps pass. Setup screen, gameplay, win flow, and return-to-setup all work correctly across multiple games with varying player counts.</done>
</task>

</tasks>

<verification>
- Setup screen appears at launch (not the board)
- Player count toggles work with visual feedback (gold=selected, neutral=unselected)
- Name fields show/hide with animation when count changes
- Names preserved when toggling count (4->2->4 keeps P3/P4 names)
- Start Game transitions to gameplay with correct players
- Win overlay New Game returns to setup (not in-place reset)
- Fade transitions between all screen states
- Score strip rebuilds when player count changes between games
- No double-fired signals (grid buttons work correctly across multiple games)
</verification>

<success_criteria>
1. At game start, player selects 2/3/4 players and enters names before board appears (SETUP-01, SETUP-02)
2. Win screen shows winner and clicking New Game returns to setup with state preserved (WIN-03)
3. Multiple consecutive games with different player counts work without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-and-game-flow/03-01-SUMMARY.md`
</output>
