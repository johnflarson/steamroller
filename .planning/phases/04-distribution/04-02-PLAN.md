---
phase: 04-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - EXPORT-01
  - EXPORT-02

must_haves:
  truths:
    - "HTML5 export loads and plays a complete game in a browser"
    - "Linux desktop build runs the full game without errors"
    - "Windows desktop build runs the full game without errors"
    - "deploy.sh runs to completion distributing to all three destinations"
  artifacts: []
  key_links: []

user_setup:
  - service: godot-editor
    why: "Manual export of HTML5, Linux, and Windows builds"
    dashboard_config:
      - task: "Open project in Godot 4.6 editor"
        location: "Godot Editor > Project > Open"
      - task: "Export Web build (preset: Steamroller Web)"
        location: "Godot Editor > Project > Export > Steamroller Web > Export Project"
      - task: "Export Linux build (preset: Linux)"
        location: "Godot Editor > Project > Export > Linux > Export Project"
      - task: "Export Windows build (preset: Windows)"
        location: "Godot Editor > Project > Export > Windows > Export Project"
  - service: itch.io
    why: "Game page must exist before butler can push"
    dashboard_config:
      - task: "Create new project at itch.io with slug 'steamroller'"
        location: "itch.io > Dashboard > Create new project"
      - task: "Set Kind to 'HTML' and Pricing to 'No payments'"
        location: "itch.io project edit page"
      - task: "Set viewport dimensions to 1280x720"
        location: "itch.io project edit page > Embed options"
      - task: "Save as Draft (initially unlisted per user decision)"
        location: "itch.io project edit page > Visibility"
  - service: butler
    why: "Automated itch.io uploads"
    env_vars:
      - name: BUTLER_API_KEY
        source: "Run 'butler login' for interactive auth, or set from itch.io API keys page"
    dashboard_config:
      - task: "Install butler and authenticate"
        location: "Terminal: see install commands in 04-RESEARCH.md"
  - service: github
    why: "GitHub Releases for desktop builds"
    dashboard_config:
      - task: "Create GitHub repo for dicegame (if not exists)"
        location: "Terminal: gh repo create dicegame --public --source=."
      - task: "Ensure gh CLI is authenticated"
        location: "Terminal: gh auth status"
  - service: itch.io-post-deploy
    why: "First butler push requires manual 'Playable in browser' flag"
    dashboard_config:
      - task: "After first deploy, mark html5 channel as 'This file will be played in the browser'"
        location: "itch.io > Edit game > Uploads section"
---

<objective>
Export builds from Godot editor, run deploy.sh, and verify game works on all target platforms.

Purpose: This is the final verification phase — the user manually exports from Godot (locked decision: no CLI export), runs the deploy script, and confirms the game plays correctly on web, Linux, and Windows.
Output: Working game distributed to luminaldata.com, itch.io, and GitHub Releases.
</objective>

<execution_context>
@/home/jlarson/.claude/get-shit-done/workflows/execute-plan.md
@/home/jlarson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution/04-CONTEXT.md
@.planning/phases/04-distribution/04-RESEARCH.md
@.planning/phases/04-distribution/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Export builds from Godot editor</name>
  <files>export/web/index.html, export/linux/steamroller.x86_64, export/windows/steamroller.exe</files>
  <action>User must manually export all three builds from the Godot editor. Claude cannot do this — locked decision is manual export from editor UI.

Steps:
1. Open the project in Godot 4.6 editor
2. Go to Project > Export
3. You should see three presets: Steamroller Web, Linux, Windows
4. For each preset, click "Export Project..." and accept the default path:
   - Steamroller Web -> export/web/index.html
   - Linux -> export/linux/steamroller.x86_64
   - Windows -> export/windows/steamroller.exe
5. Verify the files exist:
   ls -la export/web/index.html export/linux/steamroller.x86_64 export/windows/steamroller.exe</action>
  <verify>ls export/web/index.html export/linux/steamroller.x86_64 export/windows/steamroller.exe</verify>
  <done>All three export files exist and are non-zero size</done>
  <resume-signal>Type "exported" when all three builds are exported, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set up distribution prerequisites and run deploy</name>
  <files>deploy.sh</files>
  <action>User must set up external service accounts and run deploy.sh. Several steps require interactive authentication that Claude cannot perform.

Prerequisites (one-time setup):
1. Update deploy.sh — Replace ITCH_USERNAME placeholder with your actual itch.io username
2. Install butler (if not installed):
   curl -L -o /tmp/butler.zip https://broth.itch.zone/butler/linux-amd64/LATEST/archive/default
   unzip /tmp/butler.zip -d ~/bin/
   chmod +x ~/bin/butler
3. Authenticate butler: butler login (opens browser)
4. Create itch.io game page: Go to itch.io > Dashboard > Create new project
   - Title: Steamroller, Project URL slug: steamroller, Kind: HTML, Pricing: No payments, Viewport: 1280x720, Save as Draft
5. Create GitHub repo (if not exists): gh repo create dicegame --public --source=. --push
6. Verify gh auth: gh auth status
7. Verify SSH to luminaldata-prod: ssh luminaldata-prod echo "ok" (needed for Astro site deploy)

Run deploy: ./deploy.sh

After first deploy:
- Go to itch.io > Edit game > Uploads
- Find the html5 channel and check "This file will be played in the browser"</action>
  <verify>Check deploy.sh output for completion message showing version number</verify>
  <done>deploy.sh ran to completion, all builds pushed to itch.io, GitHub Release created, HTML5 copied to Astro site</done>
  <resume-signal>Type "deployed" when deploy.sh completes and itch.io is configured, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify game on all platforms</name>
  <files></files>
  <action>Verify the game works correctly on all distribution platforms. This is the final acceptance test for the entire project.

Verification steps:
1. HTML5 on Astro site: Visit https://luminaldata.com/blog/steamroller/ — game should load in iframe, start a 2-player game, play a few turns, verify scoring works
2. HTML5 on itch.io: Visit your itch.io steamroller page — click Play, verify game loads and works identically to the Astro embed
3. Linux desktop: Download from GitHub Releases or run export/linux/steamroller.x86_64 directly — verify it opens at 1280x720, play a full game
4. Windows desktop: On your WSL2 host Windows, run export/windows/steamroller.exe — verify it opens and plays correctly
5. GitHub Release: Visit the GitHub repo releases page — verify both zip files are attached with correct version</action>
  <verify>Manual verification on all 5 platforms/destinations</verify>
  <done>Game plays correctly on luminaldata.com iframe, itch.io embed, Linux native, Windows native. GitHub Release has correct assets.</done>
  <resume-signal>Type "approved" if all platforms work, or describe which platform(s) have issues</resume-signal>
</task>

</tasks>

<verification>
- HTML5 game loads and is playable at luminaldata.com/blog/steamroller/
- HTML5 game is playable on itch.io steamroller page
- Linux binary runs and plays a complete game
- Windows .exe runs and plays a complete game on WSL2 host
- GitHub Release exists with versioned zip assets
</verification>

<success_criteria>
The game "Steamroller" is distributed and verified on all target platforms: embedded on luminaldata.com, playable and downloadable on itch.io, and available as desktop builds on GitHub Releases. EXPORT-01 and EXPORT-02 requirements are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-02-SUMMARY.md`
</output>
